{
    "version":2,
    "builds": [
          { "src":"app.py",
            "use": "@vercel/python",
            "config": {
                "commands": [
                  "brew install libxml2 libxslt" 
                ]
              }
    }
           
        
    ],
    "routes": [
        {
            "src":"/(.*)",
            "dest":"app.py"
        }
    ]
}
