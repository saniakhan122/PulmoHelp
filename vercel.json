{
    "version":2,
    "builds": [
          { "src":"app.py",
            "use": "@vercel/python",
            "config": {
                "commands": [
                  "brew install libxml2 libxslt" ,
                    "pip install -r requirements.txt"
                ]
              }
    }
           
        
    ],
    "routes": [
        {
            "src":"/(.*)",
            "dest":"app.py"
        }
    ]
}
